config {
    dependencies: ["estoque_online"],
    tags: ["cubo-estoque"]
}

TRUNCATE TABLE REFINED.ESTOQUE_SAP;

INSERT INTO 
    `REFINED.ESTOQUE_SAP` (
SELECT * 
FROM REFINED.ESTOQUE_ONLINE
WHERE DAT_ESTOQUE = DATE_SUB(CURRENT_TIMESTAMP(), INTERVAL 1 DAY)
);